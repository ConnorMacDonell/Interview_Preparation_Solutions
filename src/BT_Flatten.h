/*
 * BT_Flatten.h
 *
 *  Created on: Jun 4, 2019
 *      Author: Connor
 */

/*
 * Problem Description
 * Given the root to a binary tree flatten it into a degenerate tree. That is every node should be the
 * right child of its parent. Do this in place, the resulting tree should be in the order of an inorder
 * traversal of the tree
 */

#include "BT_Node.h"

#ifndef BT_FLATTEN_H_
#define BT_FLATTEN_H_





#endif /* BT_FLATTEN_H_ */
